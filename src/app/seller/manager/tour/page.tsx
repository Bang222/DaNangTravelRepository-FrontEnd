import {FC} from 'react';
import TourManager from "@/components/seller/tourManager";

interface PageProps {
}

//bang

const Page: FC<PageProps> = ({}) => {
    return (
        <TourManager/>
    );
}

export default Page;
